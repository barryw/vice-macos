apiVersion: v1
kind: Service
metadata:
  name: macvice-website
  labels:
    app.kubernetes.io/name: macvice-website
    app.kubernetes.io/component: website
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: macvice-website
    app.kubernetes.io/component: website
  ports:
    - name: http
      port: 80
      targetPort: http
